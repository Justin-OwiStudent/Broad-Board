import React from 'react';
import classes from './Cart.module.css'

const Cart = () => {
    return (
        <div className={classes.cart}>
            
        </div>
    );
};

export default Cart;